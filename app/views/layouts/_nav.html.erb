  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to 'Finance Tracker', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                  aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item active">
          <%= link_to 'Friends', my_friends_path, class:"nav-link #{'active' if request.path == my_friends_path }" %>
        </li>
        <li class="nav-item active">
          <%= link_to 'Portfolio', my_portfolio_path, class:"nav-link #{'active' if request.path == my_portfolio_path }" %>
        </li>
        <li class="nav-item dropdown">
          <%= link_to '', id: "navbar-dropdown", class: "nav-link dropdown-toggle", data: { 'bs-toggle': "dropdown" },
                          aria: { haspopup: true, expanded: false } do %>
            Account
          <% end %>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if !user_signed_in? %>
              <%= link_to 'Sign Up', new_user_registration_path, class: 'dropdown-item' %>
              <a class="dropdown-item" href="#">Login</a>
            <% end %>
            <% if user_signed_in? %>
                <%= link_to edit_user_registration_path, class: "dropdown-item"  do%>
                  <i class="bi bi-person"></i> <%= current_user.full_name %>
                <% end %>
                <%= link_to 'Sign out', destroy_user_session_path, data: {turbo_method: :delete}, class: 'dropdown-item' %>
              <% end %>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </nav>
